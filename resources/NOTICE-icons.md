# Icon Notice

The app icon uses a notebook mark adapted from the Lucide `notebook-text` icon.

- Source: https://lucide.dev/
- License: ISC
- Local package: `lucide-react`

The generated `icon.ico` is used by `electron-builder` for Windows packaging.
